# Edge requirements — Hailo backend only (INFERENCE_BACKEND=hailo)
# Use this instead of requirements.txt on Hailo-equipped edge devices.
# Removes torch/ultralytics to keep the image lightweight.

# API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9
requests>=2.32.0
python-dotenv>=1.0.1

# System Utilities
psutil>=6.0.0

# Database
sqlalchemy>=2.0.36
psycopg2-binary>=2.9.10

# Core numeric
numpy>=2.1.0

# OpenCV (headless for server deployment)
opencv-python-headless>=4.10.0.84

# ultralytics  # NOT needed — Hailo backend does not use PyTorch/YOLO

# hailo_platform is NOT on PyPI — install via HailoRT SDK.
# Place your compiled .hef file in ./models/ and set HAILO_HEF_PATH=./models/your_model.hef
